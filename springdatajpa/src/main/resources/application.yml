spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://192.168.1.151:3306/jpa?useSSL=false&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
    # 更新或创建数据表
      ddl-auto: update
    # 控制台显示SQL
    show-sql: true

logging:
  level:
    root: info

sonic:
  hello:
    prefix: sonic
    suffix: hello world
